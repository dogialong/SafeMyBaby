<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.safemybaby.vtree.safemybaby">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".HandbookCateScreen" />
        <activity android:name=".QuestionScreen" />
        <activity android:name=".FlashCardScreen" />
        <!-- <activity android:name=".QuestionScreen"/> -->
        <activity android:name=".SelectModePlayScreen" />
        <activity android:name=".DetailHandbookScreen" />
        <activity android:name=".ContentHandbookScreen" />
        <activity android:name=".QuestionCateScreen"></activity>
    </application>

</manifest>